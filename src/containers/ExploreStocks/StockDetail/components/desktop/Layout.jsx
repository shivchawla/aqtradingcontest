import React from 'react';

export default class Layout extends React.Component {
    render() {
        return(
            <h1>This is the layout for the desktop StockDetail component</h1>
        );
    }
}