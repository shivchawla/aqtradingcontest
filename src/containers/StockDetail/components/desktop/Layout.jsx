import React from 'react';
import styled from 'styled-components';

export default class Layout extends React.Component {
    render() {
        return (
            <div>This the the Stock DetailComponent for mobile</div>
        );
    }
}